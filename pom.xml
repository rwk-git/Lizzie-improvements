<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>featurecat</groupId>
    <artifactId>lizzie</artifactId>
    <version>0.7.3</version>

    <properties>
        <!-- Prevent warning: "[WARNING] Using platform encoding (UTF-8 actually) to copy filtered resources, i.e. build is platform dependent!"-->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.7.0</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <compilerArgs>
                      <arg>-Xlint:all</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.0.2</version>
                <!-- No need to specify the main class since maven-shade-plugin will add it. -->
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer
                                        implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>featurecat.lizzie.Lizzie</mainClass>
                                </transformer>
                            </transformers>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.9</version>
                <configuration>
                    <!-- Required to get the test failure reports in stdout -->
                    <useFile>false</useFile>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.coveo</groupId>
                <artifactId>fmt-maven-plugin</artifactId>
                <version>2.5.1</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>format</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>sh.tak.appbundler</groupId>
                <artifactId>appbundle-maven-plugin</artifactId>
                <version>1.2.0</version>
                <configuration>
                    <mainClass>featurecat.lizzie.Lizzie</mainClass>
                    <iconFile>assets/icon.icns</iconFile>
                    <dictionaryFile>config/Info.plist</dictionaryFile>
                </configuration>
                <executions>
                    <execution>
                    <phase>package</phase>
                    <goals>
                        <goal>bundle</goal>
                    </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.codehaus.plexus</groupId>
                        <artifactId>plexus-archiver</artifactId>
                        <version>4.8.0</version>
                    </dependency>
                    <dependency>
                        <groupId>org.codehaus.plexus</groupId>
                        <artifactId>plexus-velocity</artifactId>
                        <version>1.2</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>build-native-launcher-and-sign</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <property name="app.dir"
                                          value="${project.build.directory}/${project.artifactId}-${project.version}/${project.artifactId}.app"/>

                                <!-- Compile a universal (arm64 + x86_64) native launcher binary
                                     to replace the x86_64-only binary from appbundle-maven-plugin.
                                     Requires Xcode Command Line Tools (clang). -->
                                <exec executable="clang" failonerror="true">
                                    <arg value="-arch"/>    <arg value="arm64"/>
                                    <arg value="-arch"/>    <arg value="x86_64"/>
                                    <arg value="-o"/>
                                    <arg value="${app.dir}/Contents/MacOS/JavaAppLauncher"/>
                                    <arg value="${project.basedir}/src/macos/launcher.c"/>
                                </exec>

                                <!-- PkgInfo is required by macOS Launch Services but is not
                                     created by appbundle-maven-plugin 1.2.0. -->
                                <echo file="${app.dir}/Contents/PkgInfo"
                                      message="APPL????"/>

                                <!-- Remove any files at the .app root that aren't Contents/
                                     (e.g. config.txt / persist left by a previous run of the app).
                                     Such "unsealed" files cause codesign to fail. -->
                                <exec executable="/bin/bash" failonerror="false">
                                    <arg value="-c"/>
                                    <arg value="find '${app.dir}' -maxdepth 1 -mindepth 1 -not -name 'Contents' -delete"/>
                                </exec>

                                <!-- Ad-hoc sign the whole bundle so macOS (Gatekeeper / spctl)
                                     accepts it without a paid Apple Developer certificate.
                                     On first launch macOS may still prompt in
                                     System Settings → Privacy & Security → allow anyway. -->
                                <exec executable="codesign" failonerror="true">
                                    <arg value="--force"/>
                                    <arg value="--deep"/>
                                    <arg value="--sign"/>
                                    <arg value="-"/>
                                    <arg value="${app.dir}"/>
                                </exec>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <!-- https://mvnrepository.com/artifact/org.json/json -->
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>20231013</version>
        </dependency>

        <!-- https://mvnrepository.com/artifact/junit/junit -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.1</version>
            <scope>test</scope>
        </dependency>

        <!-- https://code.google.com/archive/p/juniversalchardet -->
        <dependency>
            <groupId>com.googlecode.juniversalchardet</groupId>
            <artifactId>juniversalchardet</artifactId>
            <version>1.0.3</version>
        </dependency>

        <!-- https://mvnrepository.com/artifact/com.jhlabs/filters -->
        <dependency>
            <groupId>com.jhlabs</groupId>
            <artifactId>filters</artifactId>
            <version>2.0.235</version>
        </dependency>

        <dependency>
            <groupId>org.java-websocket</groupId>
            <artifactId>Java-WebSocket</artifactId>
            <version>1.5.0</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>1.7.25</version>
        </dependency>

        <dependency>
            <groupId>io.socket</groupId>
            <artifactId>socket.io-client</artifactId>
            <version>2.0.1</version>
        </dependency>
    </dependencies>
</project>
